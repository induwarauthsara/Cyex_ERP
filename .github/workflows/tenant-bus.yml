on: push
name: 🚀 Deploy bus on push
jobs:
  web-deploy:
    name: 🎉 Deploy bus
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: 198.37.120.210
          username: bus@bus.cyextech.com
          password: MySecure@Pass456
          server-dir: /
